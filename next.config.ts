import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* Top-level root property */
    allowedDevOrigins: ['192.168.29.171', 'localhost:3000'],

    /* Clear or keep any other config options here */
};

export default nextConfig;